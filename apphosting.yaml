# Firebase App Hosting Configuration
# See https://firebase.google.com/docs/app-hosting/configure

kind: "AppHostingBackend"
schemaVersion: "v1"

# The directory containing the application code (relative to git root)
# In this project, the Next.js app lives in the 'website' directory.
buildSettings:
  rootDir: "website"
