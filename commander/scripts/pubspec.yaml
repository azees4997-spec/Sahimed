name: sahimed_uploader
description: "Automation script to upload Sahimed AAB to Google Play."
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  googleapis: ^14.0.0
  googleapis_auth: ^1.6.0
  path: ^1.9.0
